package net.dollar.simplegear.config;

/**
 * Handles new client-only configs for the mod, which can be used anywhere for any purpose (ex. models
 * and rendering).
 */
public class ModClientConfigs {
    //NONE HERE, COMMON ONLY
}
